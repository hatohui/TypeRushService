/**
 * DTO for updating an existing user achievement
 * Note: User achievements have no updatable fields (composite key only)
 */
export class UpdateUserAchievementDto {
  // No updatable fields for user achievements
}
