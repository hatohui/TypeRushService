# .gitignore for a mixed .NET, Node.js and Python (FastAPI) repository

# === OS ===
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
# macOS
.DS_Store
.AppleDouble
.LSOverride
# Linux swap / temp
*~ 
.nfs*

# === IDEs / Editors ===
.vscode/
.vscode-test/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates
*.iml

# === .NET / Visual Studio ===
# Build outputs
bin/
obj/
out/
publish/
# Rider
.idea/
# NuGet
*.nupkg
packages/
project.lock.json
# Visual Studio temporary files, build results, and
*.ilk
*.meta
*.obj
*.pdb
*.cache
*.log
*.vs/
# Test results
TestResults/
*.trx

# ASP.NET / app settings that might contain secrets
appsettings.*.Local.json
appsettings.*.Secret.json

# Dotnet user-secrets file (if present locally)
secrets.json

# === Node.js ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# build / bundler outputs
dist/
build/
.cache/
.next/
.out/
public/build/
.esbuild/
parcel-cache/
# dotenv
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# lockfiles - typically you should commit lockfiles; uncomment only if you want to ignore
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# coverage
coverage/
.nyc_output/

# === Python / FastAPI ===
# Byte-compiled / optimized / cache
__pycache__/
*.py[cod]
*$py.class
# C extensions
*.so
# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/
# Virtual environments
venv/
.venv/
# Pytest cache
.pytest_cache/
# MyPy / type checkers
.mypy_cache/
.pyre/
.pytype/
# Coverage and test reports
.coverage
coverage.xml
htmlcov/
# Local env files
.env
.env.*.local
# SQLite DBs (if used)
*.db
*.sqlite3

# === Docker / Containers ===
docker-compose.override.yml
docker-compose.*.yml
**/Dockerfile.*.local

# === Logs / temp / misc ===
logs/
*.log
*.tmp
tmp/
temp/
.cache/
*.bak
*.swp

# === Secrets / Keys / Certificates ===
*.pem
*.key
*.crt
prompt.md
# End of file